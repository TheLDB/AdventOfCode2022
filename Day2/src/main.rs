use std::fs;

fn part_one() {
    let input = fs::read_to_string("./src/input").unwrap().split("\n");

    for(index, elemenet) in input.enumerate() {

    }
}

fn part_two() {
    let input = fs::read_to_string("./src/input").unwrap().split("\n");

    for(index, elemenet) in input.enumerate() {

    }
}

fn main() {
    let part_one = part_one();
    println!("{:#?}", part_one);
}